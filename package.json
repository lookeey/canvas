{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "interface",
      "uikit"
    ]
  },
  "scripts": {
    "build": "cd ./uikit && yarn build && cd ../interface && yarn build"
  },
  "dependencies": {
    "styled-components": "^5.3.6"
  }
}
